---
title: Popover
description: Popover component built on Base UI primitives.
component: true
base: base
---

## Installation

```bash
npx shadcn@latest add https://ui-ecosystem.dev/r/popover.json
```

The component source is published as a registry payload at
`/r/popover.json`.

## Usage

```tsx
import { Popover, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger } from "@/components/ui/popover";
```

```tsx
<Popover />
```

## Dependencies

`@base-ui/react`, `react`
